1 注册：
    1. 前端 - 填写用户名和密码，点击注册，发送给后端
    3. 后端 - 接收请求，接收参数，把数据存入数据库，根据结果判断，响应数据给前端
    5. 前端 - 接收后端响应数据，判断，弹出提示


2 登录
    1. 前端 - 填写用户名和密码，点击登录，发送给后端
    2. 后端 - 接收参数，去数据库查询是否存在，根据结果判断，响应数据给前端
    3. 前端 - 接收数据，判断，弹出提示

